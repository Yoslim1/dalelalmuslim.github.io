import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { useMemo } from "react";
import { ActivityIndicator, Pressable, StyleSheet, Text, View } from "react-native";

import { useChapterAudioAction } from "@/components/audio/use-chapter-audio-action";
import { Surface } from "@/components/dalil-ui";
import { useQuranAudio } from "@/lib/audio/player";
import { useReciterLibrary } from "@/lib/audio/reciter-library";
import { palette } from "@/lib/ui/theme";

type Props = { chapter: number; chapterName: string };
const speeds = [0.75, 1, 1.25, 1.5] as const;

export function QuranAudioPanel({ chapter, chapterName }: Props) {
  const { playback, seekBy, setSpeed, togglePlayback } = useQuranAudio();
  const { selectedReciter, reciterManifest } = useReciterLibrary();
  const action = useChapterAudioAction(chapter, chapterName);
  const activeTrack = playback.track?.chapter === chapter ? playback.track : null;
  const timeLabel = useMemo(() => `${formatTime(playback.positionMs)} / ${formatTime(playback.durationMs)}`, [playback.durationMs, playback.positionMs]);
  const attribution = reciterManifest?.reciter.license.attribution;

  return <Surface style={styles.card}>
    <View style={styles.heading}><View style={styles.headingCopy}><Text style={styles.title}>التلاوة الصوتية</Text><Text style={styles.subtitle}>{activeTrack ? activeTrack.reciterName : selectedReciter ? `القارئ المختار: ${selectedReciter.nameAr}` : "اختر قارئًا من فهرس السور"}</Text></View><View style={styles.iconWrap}><MaterialIcons color={palette.primary} name="headphones" size={22} /></View></View>
    {activeTrack ? <PlayerControls timeLabel={timeLabel} playback={playback} seekBy={seekBy} setSpeed={setSpeed} togglePlayback={togglePlayback} /> : <ChapterControls actionState={action.actionState} message={action.message} onDownload={action.download} onPlay={action.play} onRemove={action.remove} />}
    {attribution ? <Text style={styles.attribution}>المصدر: {attribution}</Text> : null}
  </Surface>;
}

function ChapterControls({ actionState, message, onDownload, onPlay, onRemove }: { actionState: ReturnType<typeof useChapterAudioAction>["actionState"]; message: string | null; onDownload: () => Promise<void>; onPlay: () => Promise<void>; onRemove: () => Promise<void> }) {
  if (actionState === "unavailable") return <View style={styles.empty}><Text style={styles.emptyText}>لا توجد تلاوة منشورة لهذا القارئ لهذه السورة بعد.</Text><Text style={styles.emptyDetail}>اختر القارئ من فهرس السور، وستظهر أزرار تنزيل أو تشغيل للسور التي يتيحها فقط.</Text></View>;
  if (actionState === "downloading") return <View style={styles.busy}><ActivityIndicator color={palette.primary} /><Text style={styles.busyText}>جارٍ التحقق من النص وتنزيل التلاوة…</Text></View>;
  if (actionState === "play") return <View style={styles.actionRow}><Pressable accessibilityLabel="تشغيل التلاوة المحفوظة" onPress={() => void onPlay()} style={({ pressed }) => [styles.primaryAction, pressed && styles.pressed]}><MaterialIcons color="#FFFFFF" name="play-arrow" size={23} /><Text style={styles.primaryActionText}>تشغيل التلاوة</Text></Pressable><Pressable accessibilityLabel="حذف التلاوة المحفوظة" onPress={() => void onRemove()} style={({ pressed }) => [styles.iconAction, pressed && styles.pressed]}><MaterialIcons color={palette.danger} name="delete-outline" size={22} /></Pressable>{message ? <Text style={styles.status}>{message}</Text> : null}</View>;
  const retry = actionState === "error";
  return <View style={styles.downloadBlock}><Pressable accessibilityLabel={retry ? "إعادة تنزيل التلاوة" : "تنزيل التلاوة"} onPress={() => void onDownload()} style={({ pressed }) => [styles.downloadAction, retry && styles.retryAction, pressed && styles.pressed]}><MaterialIcons color={retry ? palette.danger : "#FFFFFF"} name={retry ? "refresh" : "download"} size={21} /><Text style={[styles.primaryActionText, retry && styles.retryText]}>{retry ? "إعادة تنزيل التلاوة" : "تنزيل التلاوة"}</Text></Pressable>{message ? <Text style={styles.status}>{message}</Text> : null}</View>;
}

function PlayerControls({ playback, timeLabel, seekBy, setSpeed, togglePlayback }: { playback: ReturnType<typeof useQuranAudio>["playback"]; timeLabel: string; seekBy: (value: number) => Promise<void>; setSpeed: (value: number) => void; togglePlayback: () => void }) {
  return <><View style={styles.progressTrack}><View style={[styles.progressValue, { width: `${Math.min(100, playback.durationMs ? (playback.positionMs / playback.durationMs) * 100 : 0)}%` }]} /></View><Text style={styles.time}>{timeLabel}</Text><View style={styles.playerControls}><Pressable accessibilityLabel="تأخير 10 ثوان" onPress={() => void seekBy(-10000)} style={({ pressed }) => [styles.iconButton, pressed && styles.pressed]}><MaterialIcons color={palette.primary} name="replay-10" size={24} /></Pressable><Pressable accessibilityLabel={playback.playing ? "إيقاف مؤقت" : "تشغيل"} onPress={togglePlayback} style={({ pressed }) => [styles.playButton, pressed && styles.pressed]}><MaterialIcons color="#FFFFFF" name={playback.playing ? "pause" : "play-arrow"} size={30} /></Pressable><Pressable accessibilityLabel="تقديم 10 ثوان" onPress={() => void seekBy(10000)} style={({ pressed }) => [styles.iconButton, pressed && styles.pressed]}><MaterialIcons color={palette.primary} name="forward-10" size={24} /></Pressable></View><View style={styles.speedRow}><Text style={styles.speedLabel}>السرعة</Text><View style={styles.speedChoices}>{speeds.map((speed) => <Pressable key={speed} accessibilityLabel={`سرعة ${speed}x`} onPress={() => setSpeed(speed)} style={({ pressed }) => [styles.speedChip, playback.speed === speed && styles.speedChipActive, pressed && styles.pressed]}><Text style={[styles.speedText, playback.speed === speed && styles.speedTextActive]}>{speed}×</Text></Pressable>)}</View></View></>;
}

function formatTime(milliseconds: number) { const total = Math.max(0, Math.floor(milliseconds / 1000)); return `${String(Math.floor(total / 60)).padStart(2, "0")}:${String(total % 60).padStart(2, "0")}`; }

const styles = StyleSheet.create({
  card: { marginHorizontal: 20, padding: 18 }, heading: { alignItems: "center", flexDirection: "row", justifyContent: "space-between" }, headingCopy: { alignItems: "flex-end", flex: 1, marginLeft: 12 }, title: { color: palette.ink, fontSize: 17, fontWeight: "900", textAlign: "right" }, subtitle: { color: palette.muted, fontSize: 12, lineHeight: 19, marginTop: 3, textAlign: "right" }, iconWrap: { alignItems: "center", backgroundColor: palette.primarySoft, borderRadius: 16, height: 44, justifyContent: "center", width: 44 }, empty: { alignItems: "flex-end", backgroundColor: palette.background, borderRadius: 16, marginTop: 16, padding: 14 }, emptyText: { color: palette.ink, fontSize: 13, fontWeight: "800", lineHeight: 22, textAlign: "right" }, emptyDetail: { color: palette.muted, fontSize: 12, lineHeight: 19, marginTop: 6, textAlign: "right" }, busy: { alignItems: "center", flexDirection: "row", gap: 10, justifyContent: "center", marginTop: 18, minHeight: 48 }, busyText: { color: palette.muted, fontSize: 13, fontWeight: "700" }, downloadBlock: { marginTop: 16 }, downloadAction: { alignItems: "center", backgroundColor: palette.primary, borderRadius: 24, flexDirection: "row", gap: 8, justifyContent: "center", minHeight: 48, paddingHorizontal: 20 }, retryAction: { backgroundColor: "#FCE9E9" }, retryText: { color: palette.danger }, actionRow: { alignItems: "center", flexDirection: "row", flexWrap: "wrap", gap: 10, marginTop: 16 }, primaryAction: { alignItems: "center", backgroundColor: palette.primary, borderRadius: 24, flex: 1, flexDirection: "row", gap: 8, justifyContent: "center", minHeight: 48 }, primaryActionText: { color: "#FFFFFF", fontSize: 14, fontWeight: "900" }, iconAction: { alignItems: "center", backgroundColor: "#FCE9E9", borderRadius: 24, height: 48, justifyContent: "center", width: 48 }, status: { color: palette.muted, flexBasis: "100%", fontSize: 12, lineHeight: 19, textAlign: "right" }, attribution: { color: palette.muted, fontSize: 10, lineHeight: 16, marginTop: 10, textAlign: "right" }, progressTrack: { backgroundColor: palette.primarySoft, borderRadius: 999, height: 6, marginTop: 18, overflow: "hidden", width: "100%" }, progressValue: { backgroundColor: palette.primary, borderRadius: 999, height: "100%" }, time: { color: palette.muted, fontSize: 12, marginTop: 7, textAlign: "center", writingDirection: "ltr" }, playerControls: { alignItems: "center", flexDirection: "row", gap: 28, justifyContent: "center", marginTop: 12, writingDirection: "ltr" }, iconButton: { alignItems: "center", borderRadius: 24, height: 48, justifyContent: "center", width: 48 }, playButton: { alignItems: "center", backgroundColor: palette.primary, borderRadius: 28, height: 56, justifyContent: "center", width: 56 }, speedRow: { alignItems: "center", flexDirection: "row", justifyContent: "space-between", marginTop: 14 }, speedLabel: { color: palette.muted, fontSize: 12, fontWeight: "800", textAlign: "right" }, speedChoices: { flexDirection: "row", gap: 6, writingDirection: "ltr" }, speedChip: { alignItems: "center", backgroundColor: palette.primarySoft, borderRadius: 12, justifyContent: "center", minHeight: 38, minWidth: 45, paddingHorizontal: 8 }, speedChipActive: { backgroundColor: palette.primary }, speedText: { color: palette.primary, fontSize: 12, fontWeight: "900" }, speedTextActive: { color: "#FFFFFF" }, pressed: { opacity: 0.75, transform: [{ scale: 0.97 }] },
});
